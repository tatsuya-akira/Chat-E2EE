<Window x:Class="Hermes.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cài đặt" Height="450" Width="350"
        WindowStartupLocation="CenterOwner" Background="#F9FAFB" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="Button" x:Key="ModernButtonStyle">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Cài đặt tài khoản" FontSize="20" FontWeight="Bold" Foreground="#1F2937" HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <StackPanel Grid.Row="1">
            <Ellipse Width="80" Height="80" Fill="#E5E7EB" HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Text="Thông tin định danh" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12" Margin="0,10,0,5"/>
            <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock x:Name="txtUserName" Text="Tên: loading..." Foreground="#111827" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock x:Name="txtUserEmail" Text="Email: loading..." Foreground="#4B5563" FontWeight="Medium" Margin="0,0,0,8"/>
                    <TextBlock x:Name="txtUserId" Text="ID: ..." Foreground="#9CA3AF" FontSize="12"/>
                </StackPanel>
            </Border>

            <Button Content="🔑 Xem khóa bảo mật (RSA)" Click="btnSettings_Click" 
                    Background="#3B82F6" Foreground="White" FontWeight="SemiBold" 
                    Style="{StaticResource ModernButtonStyle}" Margin="0,25,0,0"/>
        </StackPanel>

        <Button x:Name="btnLogout" Grid.Row="2" Content="Đăng xuất" Height="42" 
                Background="#FEE2E2" Foreground="#EF4444" FontWeight="Bold" 
                Style="{StaticResource ModernButtonStyle}" Click="btnLogout_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>